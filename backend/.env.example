# Rode `docker compose up -d` neste diretório para subir um Postgres local
# compatível com esses valores.
DATABASE_URL="postgresql://reclicla:reclicla@localhost:5433/reclicla"
JWT_SECRET="troque-por-um-segredo-forte-em-producao"
PORT=4000
